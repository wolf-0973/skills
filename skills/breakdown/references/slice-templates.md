# Slices: <summary>

Work the **frontier**: any slice whose blockers are all done.

## <Title>

**What to build:** end-to-end behaviour from the user's perspective.

**Blocked by:** None — can start immediately | <other titles>

**Status:** ready | in progress — <做到哪、下一步碰哪> | blocked — <原因> | done

- [ ] Acceptance criterion 1
- [ ] Acceptance criterion 2

`Status` 由 `/implement` 维护，`/breakdown` 一律写 `ready`。**`in progress` 必须带一句「做到哪」**——新会话靠它分辨「还没开始」和「做了一半」，只写 `in progress` 等于没写。
